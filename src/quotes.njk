---
layout: base.njk
title: Daily Inspiration Archive

---

<section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <h1 class="text-3xl font-bold mb-8 text-center text-gray-800">Daily Quote Archive</h1>

  <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
    {% for quote in pagedQuotes %}
      <li class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow">
        <a href="{{ quote.url }}" class="block h-full">
          <img src="{{ quote.data.image }}" alt="Quote Image" class="w-full h-48 object-cover">
          <div class="p-4">
            <p class="text-gray-700 text-base italic truncate">
              {{ quote.templateContent | striptags | truncate(100) }}
            </p>
          </div>
        </a>
      </li>
    {% endfor %}
  </ul>

  <div class="flex justify-between items-center mt-12 text-gray-600 text-sm">
    {% if pagination.href.previous %}
      <a href="{{ pagination.href.previous }}" class="hover:text-blue-500 transition">← Newer</a>
    {% else %}
      <span></span>
    {% endif %}
    
    {% if pagination.href.next %}
      <a href="{{ pagination.href.next }}" class="hover:text-blue-500 transition">Older →</a>
    {% endif %}
  </div>
</section>
